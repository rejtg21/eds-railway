# Base URL of the NestJS API on Railway. Inlined into the browser bundle at
# build time, so the web app calls the API directly (no Vercel proxy).
NEXT_PUBLIC_API_URL=http://localhost:3001

# Port Next.js listens on (Railway injects PORT automatically).
PORT=3000
